# Python bytecode
__pycache__/
*.py[cod]

# Test / tooling caches
.pytest_cache/

# Runtime ledger (generated by the CLI; not committed)
ledger.json

# Runtime custom rebalancing targets (copy targets.sample.json to create)
targets.json

# Runtime staking config and rewards (copy *.sample.* to create)
staking.json
rewards.csv

# Runtime news config (copy news.sample.json to create)
news.json

# Runtime portfolio snapshots (appended by the history subcommand)
snapshots.jsonl
.venv/
dist/
build/
*.egg-info/
